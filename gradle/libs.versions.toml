[versions]
# Spring Boot
spring-boot-version = "4.0.5"

# Discord
jda-version = "6.4.1"

# OpenAI
openai-version = "4.30.0"

# Gson
gson-version = "2.13.2"

[libraries]
# Spring Boot
spring-boot-starter = { group = "org.springframework.boot", name = "spring-boot-starter", version.ref = "spring-boot-version" }
spring-boot-bom = { group = "org.springframework.boot", name = "spring-boot-dependencies", version.ref = "spring-boot-version" }
spring-boot-starter-test = { group = "org.springframework.boot", name = "spring-boot-starter-test", version.ref = "spring-boot-version" }

# Discord
jda = { group = "net.dv8tion", name = "JDA", version.ref = "jda-version" }

# OpenAI
openai = { group = "com.openai", name = "openai-java", version.ref = "openai-version" }

#Gson
gson = { group = "com.google.code.gson", name = "gson", version.ref = "gson-version" }


# junit
junit-platform-launcher = { group = "org.junit.platform", name = "junit-platform-launcher"}

[bundles]
[plugins]